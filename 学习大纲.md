# mini-vllm 学习大纲

> 基于 [`mini-vllm-tutorial`](mini-vllm-tutorial/README.md) 整理。目标：从零实现一个 LLM 推理引擎，用 31 步（主系列 20 步 + 进阶 16 步，部分重叠）理解 vLLM 的核心设计思想——**为什么**它被这样设计，而不只是记住代码。

---

## 0. 开始之前

**核心问题**：一个参数量 700 亿、每次对话生成几百个 token 的模型，如何同时服务成千上万用户？

LLM 推理有三个根本特征，决定了它不能用通用深度学习推理框架解决：

| 特征 | 说明 |
|---|---|
| 自回归生成 | 第 N+1 个 token 必须等第 N 个算完，过程严格串行 |
| 计算/内存随长度增长 | Prefill 是 O(n²)，Decode 每步 O(n) 但需缓存全部历史 K/V |
| 生成长度未知 | 无法提前精确分配显存，分少了 OOM，分多了浪费 |

**环境准备**：

```bash
cd mini-vllm-tutorial
pip install -r requirements-cpu.txt   # 前 8 步（Phase 0-4）够用，纯 CPU 可跑
# 需要真实模型 / GPU 优化时（Phase 6-7）：
pip install -r requirements-gpu.txt
```

**每一步的学习动作（固定套路，贯穿全程）**：

1. 读该 step 的 `README.md`（含 ASCII 图解 + 问题/方案/类比三段式）
2. 跑 `python run.py` 看实际效果
3. 读核心实现文件，理解代码怎么落地原理
4. 对比相邻两步的 diff：`diff ../stepXX/engine.py engine.py`，只看**增量改了什么**

---

## 1. 全局路线图

```
Phase 0 基础组件 (step01-04, CPU)         Tokenizer → Embedding → Attention → Transformer
Phase 1 朴素推理 (step05)                  自回归生成，暴露 O(n²) 问题
Phase 2 采样算法 (step06)                  Greedy/Temperature/Top-k/Top-p
Phase 3 KV Cache (step07-08)              单请求 O(n²)→O(n)，多请求 Static Batching
Phase 4 调度器 (step09-11)                 Continuous Batching → Chunked Prefill → Preemption
Phase 5 PagedAttention (step12-13)         分页显存 17%→96%，Prefix Cache 省 77% 计算
Phase 6 真实模型 (step14, step19, GPU推荐)  分页前缀缓存 + 接入 Qwen3-0.6B
Phase 7 高级优化 (step15-17, GPU)          FlashAttention → CUDA Graph → Tensor Parallel
Phase 8 工程化 (step18, step20)            Benchmark → OpenAI 兼容 HTTP 服务
────────────────────────────────────────────────────────────────────
进阶系列 Phase A-G (adv01-16)              量化/投机解码/RadixCache/PP/SP/DP/PD分离/MoE/LoRA/结构化输出...
```

**设计演进主线**（每一步都是对上一步遗留问题的精准回应）：

```
朴素推理 → 每步重算全部历史
  └→ KV Cache → 多请求时 GPU 槽位空转（Static Batching 的短板）
      └→ Continuous Batching → 长 Prefill 阻塞 Decode
          └→ Chunked Prefill → KV Cache 显存碎片 / OOM
              ├→ Preemption（优雅降级）
              └→ PagedAttention（分页管理）→ 注意力计算带宽成瓶颈
                  └→ FlashAttention → 工程化：多卡、服务化
```

**LLM 推理系统 ≈ 在 GPU 上重走一遍 OS 走过的路**：

| # | 推理问题 | 解决方案 | step | OS 类比 |
|---|---|---|---|---|
| 1 | 每步重算整个历史，O(n²) | KV Cache | step07 | CPU 寄存器 / L1 Cache |
| 2 | 请求完成后槽位空转 | Continuous Batching | step09 | 分时操作系统的就绪队列 |
| 3 | 长 Prefill 独占 GPU | Chunked Prefill | step10 | 时间片抢占 |
| 4 | 预留 max_len 显存，利用率~60% | PagedAttention | step12 | 虚拟内存分页（page table） |
| 5 | QK^T 反复读写 HBM，带宽瓶颈 | FlashAttention | step15 | Cache-blocking 分块矩阵乘法 |

---

## 2. 主系列详细大纲（step01–step20）

### Phase 0 — 基础组件（CPU，只需 torch）

从零搭积木，理解 Transformer 的四个基础组件，是看懂后面所有优化的前提。

| Step | 主题 | 要解决的问题 | 核心方案 | 类比 |
|---|---|---|---|---|
| `step01_tokenizer` | Token 是什么 | 文字如何变成模型能处理的整数 | BPE 字节对编码，"你好世界"→`[15267,...]` | 摩尔斯电码表 |
| `step02_embedding` | 向量空间 | 整数 ID 之间没有语义关系 | Embedding 查找表，训练出的连续向量 | 地图坐标（语义相近则位置相近） |
| `step03_attention` | 手写注意力 | 词义依赖上下文，如何动态感知 | Scaled Dot-Product Attention（Q/K/V） | 图书馆检索：带问题(Q)查资料，靠封面(K)判断相关性，取内容(V) |
| `step04_transformer` | Decoder 层 | 光有 Attention 不够，需要 MLP/残差/归一化组装成完整层 | RMSNorm→Attention→残差→RMSNorm→MLP→残差，叠 N 层 | 流水线工厂的一道工序 |

**产出检验**：能说清 tokenizer 词表怎么来的、embedding 为何要训练、Q/K/V 各自的作用、残差连接为什么能防止信息丢失。

---

### Phase 1 — 朴素推理

| Step | 主题 | 问题 | 方案 | 暴露的新问题 |
|---|---|---|---|---|
| `step05_naive` | 自回归生成 | 如何用 Transformer 生成文字 | 每次把已有序列全部输入，预测下一 token，循环 | 每步重算全部历史，O(n²)——生成第100个token比第1个贵100倍 |

---

### Phase 2 — 采样算法

| Step | 主题 | 问题 | 方案 |
|---|---|---|---|
| `step06_sampler` | logits → next_token | 每次贪心选最高概率会重复、缺乏创意 | Temperature（调节分布尖锐度）+ Top-k + Top-p/Nucleus + Gumbel-Max |

类比：Temperature=0 是每次都点菜单第一道菜；Top-p=0.9 是只在"今天大概率想吃的"几道菜里随机选。

---

### Phase 3 — KV Cache：消除重复计算

| Step | 主题 | 问题 | 方案 | 遗留问题 |
|---|---|---|---|---|
| `step07_kvcache_for_single_request` | 单请求 KV Cache | 每步重算全部历史 K/V | 缓存 past_key_values，新 token 只算增量，O(n²)→O(n) | — |
| `step08_kvcache_static_batching_for_multi_requests` | 多请求 + Static Batching | 逐条处理效率低 | Padding 到同长度，拼 batch 一次矩阵乘法 | 最短请求完成后槽位空等最长请求结束（"班车等最后一个乘客"） |

---

### Phase 4 — 调度器：让 GPU 永不空转

| Step | 主题 | 问题 | 方案 | OS 类比 |
|---|---|---|---|---|
| `step09_kvcache_continuous_batching_for_multi_requests` | Continuous Batching | Static Batching 槽位空转 | 每步检查完成请求，立刻补新请求做 prefill；Sequence 状态机 WAITING→RUNNING→FINISHED | 分时操作系统调度 |
| `step10_chunked_prefill` | Chunked Prefill | 长 prompt(如4096 token) 独占 GPU，decode 请求"卡住" | 长 prefill 切成固定 chunk（如512），每步只处理一块 | 时间片抢占 |
| `step11_preemption` | Preemption | 生成长度未知，显存运行中可能耗尽 | 按 LIFO 选最晚请求，释放其 KV Cache 插回队首，稍后重新 prefill | Linux OOM Killer |

---

### Phase 5 — PagedAttention：消除显存碎片

| Step | 主题 | 问题 | 方案 | OS 类比 |
|---|---|---|---|---|
| `step12_paged_attention` | 分页内存管理 | 预分配 max_len 连续显存，利用率仅~60%（严重时10%） | 固定大小 Block（如16 token/块），按需分配，block_table 做地址翻译 | 虚拟内存分页（page table + 物理页帧） |
| `step13_prefix_cache` | Prefix Caching | 多请求共享 System Prompt，重复计算其 KV | 相同内容 Block 只留一份物理拷贝，多请求 block_table 指向同一 Block，引用计数管理 | 共享库 / Copy-on-Write |

---

### Phase 6 — 真实模型（推荐 GPU）

| Step | 主题 | 问题 | 方案 |
|---|---|---|---|
| `step14_paged_prefix_cache` | 分页前缀缓存 | Prefix Caching 在 PagedAttention 框架下如何高效实现 | 拆解为 step14_1~14_7 子步骤（向量化写KV、index_select gather、batched attention、causal mask 优化、增量哈希、block 释放、batched forward），逐步搭建 |
| `step19_real_model` | 接入 Qwen3-0.6B | 前面用的都是随机初始化 TinyTransformer，无法生成有意义文字 | 加载 safetensors 权重，映射 HF key（`model.layers.0.self_attn.q_proj.weight`）到自定义模型属性；实现 RMSNorm、GQA、RoPE；BF16 精度 |

**核心约束**（易踩坑点）：`model.py` 中每个 `nn.Linear` 的属性名必须与权重文件 key 路径精确对应——PyTorch 只认属性名不认类名，写错就是 missing key。

---

### Phase 7 — 高级优化（需要 NVIDIA GPU）

| Step | 主题 | 问题 | 方案 | 类比 |
|---|---|---|---|---|
| `step15_flash_attention` | SRAM-aware 注意力 | QK^T 中间矩阵反复读写 HBM（显存），带宽浪费 | Q/K/V 切 tile 载入 SRAM，online softmax 在片上算完，HBM 读写 O(n²)→O(n)；`flash_attn_varlen_func` 支持变长拼接（无 padding） | CPU 分块矩阵乘法（Cache-blocking） |
| `step16_cuda_graph` | 录制重放 | Decode 每步 GPU 计算仅微秒级，但 Python→PyTorch→CUDA driver 调度要几毫秒 | 提前录制 CUDA Graph，之后 `g.replay()` 跳过调度层；按 batch size 分别录制 | io_uring 批量提交 / DMA |
| `step17_tensor_parallel` | 多 GPU 张量并行 | 大模型权重超单卡显存，或单卡算力不足 | 权重按维度切分到多卡：列并行（Q/K/V，无需通信）+ 行并行（O/MLP，一次 all_reduce） | 分布式矩阵乘法分块可加性，与 FlashAttention 同源但跨 GPU 而非跨 SRAM/HBM |

---

### Phase 8 — 工程化

| Step | 主题 | 问题 | 方案 |
|---|---|---|---|
| `step18_benchmark` | 量化优化效果 | 优化到底快了多少 | 标准化压测：固定并发/prompt长度/生成长度，测吞吐量（tok/s）与延迟（TTFT/TPOT） |
| `step20_serve` | HTTP 服务 | 推理引擎只是 Python 对象，如何被任意语言/机器调用 | FastAPI 包装，实现 OpenAI `/v1/chat/completions`（含 SSE 流式） |

---

## 3. 进阶系列大纲（advanced/，adv01–adv16）

前置：建议先学完主系列 step01–20。每步同样遵循"读README→跑run.py→读代码→比diff"。

| Phase | Step | 主题 | 收益 | 代价 | 代码深度 |
|---|---|---|---|---|---|
| A 精度与采样 | adv01_quantization | W4A16/W8A16 权重量化 | 显存↓4–8×，速度↑ | 精度损失 | 🟢真代码 |
| A | adv02_sampling_advanced | MinP / 惩罚项 / Beam Search | 输出可控 | — | 🟢 |
| B 解码加速 | adv03_speculative_decoding | 投机解码（草稿+验证） | decode 2–3× | 草稿模型开销 | 🟢 |
| B | adv04_flash_decoding | 长序列 decode 切分 | 长序列 decode↑ | 需分块 | 🟡模拟 |
| C 缓存进阶 | adv05_radix_prefix_cache | Radix Attention + CoW | 前缀复用↑ | 树管理 | 🟢 |
| D 并行进阶（承接step17 TP） | adv06_pipeline_parallel | Pipeline Parallel（微批次/1F1B） | 支持更大模型 | bubble 空转 | 🟡模拟 |
| D | adv07_sequence_parallel | Sequence Parallel | 序列维切分 | 通信开销 | 🟡模拟 |
| D | adv08_data_parallel_dplb | Data Parallel + DPLB 负载均衡 | 多副本均衡 | 副本开销 | 🟢 |
| D | adv09_tbo_dbo_overlap | 计算-通信重叠 | 重叠隐藏延迟 | 复杂调度 | 🟡模拟 |
| E 分离式架构 | adv10_pd_disaggregation | Prefill/Decode 分离 | 吞吐↑ | KV 迁移 | 🟡模拟 |
| E | adv11_afd_attention_ffn | Attention/FFN 分离 | A/F 配比均衡 | 跨设备通信 | 🟡模拟 |
| F 架构变体 | adv12_moe_eplb | MoE Top-k 路由 + EPLB | 稀疏激活省算力 | 路由+均衡开销 | 🟢 |
| F | adv13_linear_attention | 线性注意力 / SSM | 长序列 O(n) | 精度近似 | 🟢 |
| G 服务与输出 | adv14_multi_lora | Multi-LoRA 动态切换 | 多任务共享 base | 适配器管理 | 🟢 |
| G | adv15_guided_decoder | JSON/regex 结构化输出 | 结构化输出 | 约束开销 | 🟢 |
| G | adv16_function_call | Function Call / Tool Call | 工具调用 | 循环延迟 | 🟢 |

> 🟢 真代码可跑；🟡 单机 CPU 难以真实加速，用模拟器/图解讲原理，README 已如实标注。

---

## 4. 纵向主线：同一类问题在多层被反复解决

学完全部步骤后，建议按"问题"而非"步骤"重新梳理一遍，理解优化是分层叠加的：

```
「重复计算」问题：
  单请求 KV Cache（step07）  → 缓存已算 K/V，避免逐 token 重算
  Prefix Caching（step13）   → 缓存共享前缀的 K/V，避免逐请求重算
  FlashAttention（step15）   → 避免 attention 中间矩阵写回显存再读回

「内存浪费」问题：
  Static Batching（step08）  → 引入新问题：补 PAD 浪费计算
  Continuous Batching（step09）→ 短请求完成即释放槽位
  PagedAttention（step12）   → 不预分配 max_len，按需分配 Block
  FlashAttention varlen接口（step15）→ 拼接输入取代 padding，零浪费

「调度延迟」问题：
  Continuous Batching（step09） → 毫秒级：请求级调度，减少 GPU 空转
  Chunked Prefill（step10）     → 毫秒级：防止长 prefill 独占 GPU
  CUDA Graph（step16）          → 微秒级：跳过 Python/driver 调度开销
```

---

## 5. 学完后的运维知识地图（对应 SUMMARY.md）

主系列 15 步学完后再读 [`SUMMARY.md`](mini-vllm-tutorial/SUMMARY.md)，把知识转成可上线的判断力：

**关键配置**：
- KV Cache 显存预算 = `num_layers × 2(K+V) × num_kv_heads × head_dim × max_total_tokens × dtype_bytes`
- `max_num_seqs`（并发数）：太小 GPU 利用率低，太大延迟上升 + 易触发 Preemption
- `chunk_size`（Chunked Prefill）：小→decode延迟稳但长prompt TTFT增；大→反之。nano-vllm/vLLM 默认 512
- `block_size`（PagedAttention）：常用 16 或 32
- 精度：FP32（调试）→ BF16（推理标准）→ INT8/INT4（显存紧张）

**核心指标**：TTFT（首token延迟，受Prefill影响）、TPOT（单token生成时间，受Decode影响）、Throughput（tok/s）、GPU/显存利用率、Prefix Cache 命中率、Preemption 次数、Waiting Queue Length。

**瓶颈诊断树**（简化）：
```
TTFT高 → prompt长: 调小chunk_size / 开Prefix Caching；prefill本身慢: 开FlashAttention
TPOT高 → GPU利用率低: 增大max_num_seqs；GPU利用率高仍慢: 上Tensor Parallelism
Throughput低 → KV Cache利用率低: 增大max_num_seqs；队列持续增长: 扩容
Preemption频繁 → 减少max_num_seqs / 减小max_model_len / 换更大显存GPU / 降精度
```

**实际优化流程**：建基线（`step18_benchmark`）→ 用诊断树定位瓶颈 → 单变量实验（每次只改一个配置）→ 检查是否引入新的权衡（如吞吐↑但TTFT↑）。

---

## 6. 与 nano-vllm 的关系与差距

`mini-vllm-tutorial` 是 [nano-vllm](https://github.com/GeeeekExplorer/nano-vllm) 的教学版本：教学优先、每步增量清晰；nano-vllm 是生产就绪版本，~1400 tok/s。

| 特性 | mini-vllm-tutorial | nano-vllm |
|---|---|---|
| KV Cache 存储 | Python list（HF 风格） | 物理显存块，Triton kernel 写入 |
| 注意力计算 | PyTorch SDPA | FlashAttention varlen + with_kvcache |
| Batch 注意力 | 逐请求 for 循环 | 真正变长 batch，一次 kernel |
| 采样 | Python argmax/multinomial | Gumbel-Max + `@torch.compile` |
| Tensor Parallel | 单进程模拟 | multiprocessing + NCCL all_reduce |
| 性能（A100, Qwen3-0.6B）| ~50 tok/s | ~1400 tok/s（约28×差距） |

**建议**：学完本教程全部步骤后，再去读 nano-vllm 源码——每一行都能对应到这里学过的概念。

---

## 7. 建议学习节奏（按精力自行伸缩）

| 阶段 | 内容 | 环境要求 | 建议投入 |
|---|---|---|---|
| 第1周 | Phase 0-2（step01-06）：基础组件 + 朴素推理 + 采样 | CPU | 打牢 Transformer 原理 |
| 第2周 | Phase 3-5（step07-13）：KV Cache → 调度器 → PagedAttention | CPU | 核心中的核心，vLLM 精髓所在 |
| 第3周 | Phase 6（step14, step19）：真实模型接入 | 推荐GPU | 从"玩具"到"能用" |
| 第4周 | Phase 7-8（step15-18, step20）：高级优化 + 工程化 | 需NVIDIA GPU | 性能与服务化 |
| 之后 | 进阶系列 adv01-16（按兴趣挑 Phase） | 视步骤而定 | 查漏补缺，覆盖生产级细节 |

每个 Phase 结束后，回到本大纲第 4 节"纵向主线"检查是否能讲清"这一层解决了上一层的什么问题"。

---

## 8. 自查清单

- [ ] 能解释 BPE 分词、Embedding、Q/K/V 注意力、残差+归一化各自的作用（Phase 0）
- [ ] 能画出 KV Cache 如何把 O(n²) 降到 O(n)（Phase 3）
- [ ] 能说清 Static Batching → Continuous Batching → Chunked Prefill → Preemption 各解决了什么问题、留下了什么问题（Phase 4）
- [ ] 能对照 OS 虚拟内存分页解释 PagedAttention 的 block_table（Phase 5）
- [ ] 能独立把 HuggingFace safetensors 权重接入自定义模型结构（Phase 6）
- [ ] 能解释 FlashAttention 为什么减少 HBM 读写、CUDA Graph 为什么跳过调度开销（Phase 7）
- [ ] 能读懂一次 benchmark 报告，并用瓶颈诊断树定位问题（Phase 8）
- [ ] 能用「重复计算 / 内存浪费 / 调度延迟」三条主线复述整个技术栈（第4节）
